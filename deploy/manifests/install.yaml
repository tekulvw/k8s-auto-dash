# Generated by 'make manifests'. Do not edit by hand.
# Source: deploy/helm/k8s-auto-dash
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: dashboardconfigs.k8s-auto-dash.io
spec:
  group: k8s-auto-dash.io
  names:
    kind: DashboardConfig
    listKind: DashboardConfigList
    plural: dashboardconfigs
    singular: dashboardconfig
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              bookmarks:
                items:
                  properties:
                    group:
                      type: string
                    icon:
                      type: string
                    id:
                      type: string
                    name:
                      type: string
                    order:
                      type: integer
                    url:
                      type: string
                  required:
                  - id
                  - name
                  - url
                  type: object
                type: array
              groups:
                items:
                  properties:
                    id:
                      type: string
                    name:
                      type: string
                    order:
                      type: integer
                  required:
                  - id
                  - name
                  - order
                  type: object
                type: array
              settings:
                properties:
                  discovery:
                    properties:
                      gatewayClassNames:
                        items:
                          type: string
                        type: array
                      namespaceSelector:
                        description: |-
                          A label selector is a label query over a set of resources. The result of matchLabels and
                          matchExpressions are ANDed. An empty label selector matches all objects. A null
                          label selector matches no objects.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  healthCheck:
                    properties:
                      enabled:
                        type: boolean
                      insecureSkipVerify:
                        type: boolean
                      intervalSeconds:
                        type: integer
                      timeoutSeconds:
                        type: integer
                    type: object
                  theme:
                    type: string
                  title:
                    type: string
                type: object
              tiles:
                items:
                  properties:
                    description:
                      type: string
                    group:
                      type: string
                    hidden:
                      type: boolean
                    icon:
                      type: string
                    id:
                      type: string
                    insecureSkipVerify:
                      type: boolean
                    name:
                      type: string
                    order:
                      type: integer
                    url:
                      type: string
                  required:
                  - id
                  type: object
                type: array
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              discoveredTiles:
                type: integer
              lastReconciled:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
# Source: k8s-auto-dash/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: k8s-auto-dash
  namespace: k8s-auto-dash
  labels:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: k8s-auto-dash-0.1.0
---
# Source: k8s-auto-dash/templates/clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: k8s-auto-dash
  labels:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: k8s-auto-dash-0.1.0
rules:
  - apiGroups: ["gateway.networking.k8s.io"]
    resources: ["gateways", "httproutes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["k8s-auto-dash.io"]
    resources: ["dashboardconfigs"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
  - apiGroups: ["k8s-auto-dash.io"]
    resources: ["dashboardconfigs/status"]
    verbs: ["get", "update", "patch"]
---
# Source: k8s-auto-dash/templates/clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: k8s-auto-dash
  labels:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: k8s-auto-dash-0.1.0
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: k8s-auto-dash
subjects:
  - kind: ServiceAccount
    name: k8s-auto-dash
    namespace: k8s-auto-dash
---
# Source: k8s-auto-dash/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: k8s-auto-dash
  namespace: k8s-auto-dash
  labels:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: k8s-auto-dash-0.1.0
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
---
# Source: k8s-auto-dash/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-auto-dash
  namespace: k8s-auto-dash
  labels:
    app.kubernetes.io/name: k8s-auto-dash
    app.kubernetes.io/instance: k8s-auto-dash
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
    helm.sh/chart: k8s-auto-dash-0.1.0
spec:
  replicas: 1
  strategy:
    type: Recreate                # single-replica; no rolling updates
  selector:
    matchLabels:
      app.kubernetes.io/name: k8s-auto-dash
      app.kubernetes.io/instance: k8s-auto-dash
  template:
    metadata:
      labels:
        app.kubernetes.io/name: k8s-auto-dash
        app.kubernetes.io/instance: k8s-auto-dash
    spec:
      serviceAccountName: k8s-auto-dash
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: k8s-auto-dash
          image: "ghcr.io/anomalyco/k8s-auto-dash:0.1.0"
          imagePullPolicy: IfNotPresent
          args: ["--addr=:8080"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet: { path: /readyz, port: http }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            periodSeconds: 30
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: true
